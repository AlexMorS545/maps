import { createStore } from 'vuex'
import data from '../json/data.json'

export default createStore({
  state: {
    cities: [],
  },
  mutations: {
  },
  actions: {
   
  },
  getters: {
  },
  modules: {
  }
})
